export enum Gender {
    NON_SEXUAL,
    MALE,
    FEMALE,
}

export enum RoomStatus {
    NORMAL,
    REMOVED,
    BLOCKED,
}

export enum Status {
    NORMAL,
    REMOVED,
}

export enum RelationCategory {
    FRIEND_REQUEST,
    FRIEND,
    BLOCKED,
}
