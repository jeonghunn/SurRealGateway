export enum Gender {
    NON_SEXUAL,
    MALE,
    FEMALE,
}

export enum RoomStatus {
    NORMAL,
    REMOVED,
    BLOCKED,
}

export enum Status {
    NORMAL,
    REMOVED,
}

export enum UserStatus {
    NORMAL,
    REMOVED,
    DEACTIVATED,
}

export enum UserPermission {
    USER,
    ADMIN,
}

export enum RelationCategory {
    FRIEND_REQUEST,
    FRIEND,
    BLOCKED,
}
